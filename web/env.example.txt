# Build and runtime
BUILD_STANDALONE=true
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=/api/v1
# The seat platform uses the live API and database; no demo data is enabled.

# Set to false when the NestJS API, PostgreSQL and Redis are available.
# Production uses same-origin HttpOnly cookies; do not expose school credentials to client code.
INTERNAL_API_URL=http://localhost:3001/api/v1

# Optional error tracking
NEXT_PUBLIC_SENTRY_DISABLED=true
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Optional: 席定领航 AI. PLATFORM_AI_* overrides the VLM provider.
PLATFORM_AI_BASE_URL=
PLATFORM_AI_API_KEY=
PLATFORM_AI_MODEL=
PLATFORM_VLM_BASE_URL=
PLATFORM_VLM_API_KEY=
PLATFORM_VLM_MODEL=mimo-v2.5
