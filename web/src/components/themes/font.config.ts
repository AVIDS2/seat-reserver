// Keep the template's theme font variables fully offline. The theme files
// provide a system-font fallback for every named font family, which avoids
// making production builds depend on Google Fonts availability.
export const fontVariables = '';
